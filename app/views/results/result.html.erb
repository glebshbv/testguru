<h3 class="font-bold text-lg w-full border-b-2">Test Result</h3>
<div class="flex flex-col gap-1 w-full">
  <p class="font-semibold">Success Percentage:</p>
  <% success_percentage = (@test_passage.correct_questions.to_f / @test_passage.test.questions.count) * 100 %>
  <p class="<%= success_percentage >= 85 ? 'text-green-500' : 'text-red-500' %>">
    <%= success_percentage.round(2) %>%
    <% if success_percentage >= 85 %>
      <span> - Test Passed!</span>
    <% else %>
      <span> - Test Not Passed!</span>
    <% end %>
  </p>
  <%= link_to 'All Tests', tests_path, class: 'mt-4 p-2 bg-blue-600 text-xs text-white rounded-xl' %>
</div>