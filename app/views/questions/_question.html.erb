<div class="flex flex-row items-center justify-between">
  <p><%= question_counter + 1 %>: <%= question.body %></p>
  <div class="flex gap-2">
    <%= link_to 'View', question_path(question), class: "underline font-xs text-light" %>
    <%= link_to 'Edit', edit_question_path(question), class: "underline font-xs text-light" %>
    <%= button_to 'Delete', question_path(question), method: :delete, class: "underline font-xs text-light text-red-600", form: { data: { turbo_confirm: 'Are you sure you want to delete this question?' } } %>
  </div>
</div>
