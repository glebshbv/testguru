<div>
  <h1>Question number <%= @question.id %></h1>
  <p><%= @question.body %></p>
  <%= link_to 'Delete', question_path(@question), method: :delete %> |
  <%= link_to 'Back to Test Questions', test_questions_path(@question.test) %>
</div>
