<h1 class="my-4 font-normal text-gray-700">Answer Details</h1>
<div class="flex flex-col items-start justify-start gap-y-3 p-2 border rounded-lg shadow bg-slate-100 max-w-xl">
  <h3 class="font-bold text-lg w-full border-b-2">Question</h3>
  <div class="flex flex-col gap-1 w-full">
    <p class="font-semibold">Body:</p>
    <p><%= @answer.question.body %> </p>
  </div>
  <div class="flex flex-col gap-3">
    <p class="font-semibold">Answer:</p>
    <%= @answer.body %>
    <p class="font-semibold">Correct:</p>
    <%= @answer.correct %>
  </div>
  <div class="flex flex-col">
    <p class="font-semibold">Actions:</p>
    <%= button_to 'Back to Question', question_path(@answer.question), class: "mt-2 p-2 bg-green-600 text-xs text-white rounded-xl", method: :get %>
  </div>
</div>

